name: 'lightdash_demo_healthcare'
version: '1.8.0'
config-version: 2
profile: 'lightdash_demo_healthcare'
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  lightdash_demo_healthcare:
    +materialized: view